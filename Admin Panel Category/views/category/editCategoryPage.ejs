<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f2f5;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .page-wrapper {
            padding: 30px 0;
        }

        .page-breadcrumb {
            margin-bottom: 25px;
        }

        .page-title {
            font-size: 2rem;
            font-weight: 700;
            color: #343a40;
            display: flex;
            align-items: center;
        }

        .breadcrumb-item a {
            color: #007bff;
            text-decoration: none;
            transition: color 0.2s ease-in-out;
        }

        .breadcrumb-item a:hover {
            color: #0056b3;
        }

        .breadcrumb-item.active {
            color: #6c757d;
        }

        .card {
            border-radius: 0.75rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .card-body {
            padding: 2.5rem;
        }

        .card-title {
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 2rem;
            color: #343a40;
            text-align: center;
        }

        .form-group.row {
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .control-label {
            font-weight: 600;
            color: #495057;
            text-align: right;
            padding-right: 15px;
        }

        .form-control {
            border-radius: 0.35rem;
            padding: 0.75rem 1rem;
            border: 1px solid #ced4da;
            transition: border-color 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }

        .form-control:focus {
            border-color: #007bff;
            box-shadow: 0 0 0 0.25rem rgba(0, 123, 255, 0.25);
        }

        .form-control[type="file"] {
            padding-top: 0.5rem;
            padding-bottom: 0.5rem;
        }

        .invalid-feedback {
            font-size: 0.875rem;
            color: #dc3545;
            margin-top: 0.5rem;
        }

        .border-top {
            border-top: 1px solid #e9ecef !important;
            padding-top: 2rem;
            margin-top: 2rem;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            padding: 0.75rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 0.35rem;
            transition: background-color 0.15s ease-in-out, border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
            box-shadow: 0 0.25rem 0.5rem rgba(0, 123, 255, 0.2);
        }

        img {
            max-width: 150px;
            height: auto;
            margin-top: 15px;
            border-radius: 0.5rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            border: 1px solid #dee2e6;
        }

        .text-muted {
            font-size: 0.9rem;
        }
    </style>
</head>

<body>
    <%- include('../header') %>

    <div class="page-wrapper">
        <div class="page-breadcrumb">
            <div class="row">
                <div class="col-12 d-flex no-block align-items-center">
                    <h4 class="page-title">Edit Category Form</h4>
                    <div class="ms-auto text-end">
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="#">Home</a></li>
                                <li class="breadcrumb-item active" aria-current="page">
                                    Library
                                </li>
                            </ol>
                        </nav>
                    </div>
                </div>
            </div>
        </div>

        <div class="container-fluid">
            <div class="row justify-content-center">
                <div class="col-md-7 col-lg-6">
                    <div class="card">
                        <form class="form-horizontal" method="post" action="/category/updateCategory/<%= data.id %>"
                            enctype="multipart/form-data">
                            <div class="card-body">
                                <h4 class="card-title">Update Category Details</h4>

                                <div class="form-group row">
                                    <label for="c_title"
                                        class="col-sm-4 text-end control-label col-form-label">Category Title</label>
                                    <div class="col-sm-8">
                                        <input type="text" class="form-control" id="c_title" name="c_title"
                                            value="<%= data.c_title%>" placeholder="Enter Category Title" required />
                                    </div>
                                </div>

                                <div class="form-group row">
                                    <label for="c_image"
                                        class="col-sm-4 text-end control-label col-form-label">Category Image</label>
                                    <div class="col-md-8">
                                        <input type="file" class="form-control" name="c_image"
                                                id="c_image" accept="image/*" />
                                        <div class="invalid-feedback">
                                            Please choose an image file for the category.
                                        </div>

                                        <% if (data.c_image && data.c_image !== '') { %>
                                            <div class="mt-3 text-center">
                                                <p class="text-muted mb-1">Current Image:</p>
                                                <img src="../../<%= data.c_image%>" alt="Current Category Image">
                                            </div>
                                        <% } else { %>
                                            <p class="text-muted mt-3">No image uploaded yet for this category.</p>
                                        <% } %>
                                    </div>
                                </div>

                                <div class="border-top">
                                    <div class="card-body text-center">
                                        <button type="submit" class="btn btn-primary">
                                            Update Category
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <%- include('../footer') %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>

</html>