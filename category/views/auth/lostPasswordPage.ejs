<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0, minimal-ui" />
    <title>Reset Password</title>
    <!-- Tailwind CSS for utility classes and responsive design -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Font: Inter (preferred font) -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Deeper, more complex background gradient */
            background: radial-gradient(circle at top left, #e0f2f7 0%, #d8eef5 30%, #cce7f0 60%, #b8dee6 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
            padding: 20px;
            box-sizing: border-box;
            position: relative;
        }

        /* Further enhanced subtle background blobs for visual depth */
        .background-blobs span {
            content: '';
            position: absolute;
            display: block;
            border-radius: 50%;
            filter: blur(180px); /* Even more blur for ethereal look */
            z-index: -1;
            opacity: 0.35; /* Slightly less opaque for true subtlety */
            animation: blobFloating 14s infinite alternate ease-in-out; /* Slower, smoother animation */
        }

        .background-blobs span:nth-child(1) {
            top: -200px; left: -200px; width: 500px; height: 500px;
            background: rgba(173, 216, 230, 0.45); /* Light blue, slightly more vibrant */
            animation-delay: 0s;
        }
        .background-blobs span:nth-child(2) {
            bottom: -220px; right: -220px; width: 550px; height: 550px;
            background: rgba(255, 223, 0, 0.18); /* Subtle yellow tint */
            animation-delay: 3s;
        }
        .background-blobs span:nth-child(3) {
            top: 70px; right: -120px; width: 220px; height: 220px;
            background: rgba(135, 206, 250, 0.35); /* Sky blue */
            animation-delay: 7s;
        }
         .background-blobs span:nth-child(4) {
            bottom: -70px; left: -100px; width: 200px; height: 200px;
            background: rgba(200, 150, 255, 0.25); /* Hint of purple */
            animation-delay: 11s;
        }

        @keyframes blobFloating {
            0% { transform: translate(0, 0) scale(1); }
            25% { transform: translate(30px, -35px) scale(1.04); } /* Slightly more movement */
            50% { transform: translate(-25px, 30px) scale(0.97); }
            75% { transform: translate(35px, -20px) scale(1.03); }
            100% { transform: translate(0, 0) scale(1); }
        }

        .container {
            width: 100%;
            max-width: 580px; /* Slightly increased max-width for grander feel */
            margin: auto;
            padding: 0;
        }

        .card {
            background: rgba(255, 255, 255, 0.99); /* Nearly full white for pristine look */
            border-radius: 30px; /* More prominent rounded corners */
            box-shadow: 0 25px 90px rgba(0, 0, 0, 0.18); /* Deeper, more elegant shadow */
            padding: 55px; /* Increased padding for luxury feel */
            animation: fadeInScaleUp 0.9s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards; /* Smoother, bouncier entrance */
            transition: transform 0.4s ease-in-out, box-shadow 0.4s ease-in-out;
            border: 1px solid rgba(255, 255, 255, 0.95); /* A subtle, almost translucent white border */
            position: relative;
            z-index: 10;
            overflow: hidden;
            background-clip: padding-box; /* Crucial for border transparency and shadow */
            -webkit-backdrop-filter: blur(25px); /* Ensure consistent blur */
            backdrop-filter: blur(25px);
        }
        .card:hover {
            transform: translateY(-12px); /* More significant lift */
            box-shadow: 0 35px 120px rgba(0, 0, 0, 0.25); /* More intense, diffused shadow on hover */
        }
        @keyframes fadeInScaleUp {
            from { opacity: 0; transform: translateY(50px) scale(0.88); } /* More initial movement and smaller scale */
            to { opacity: 1; transform: translateY(0) scale(1); }
        }

        .auth-logo {
            max-width: 180px; /* Larger logo */
            height: auto;
            margin: 0 auto 45px auto; /* More bottom margin */
            display: block;
            transition: transform 0.3s ease; /* Logo hover animation */
        }
        .auth-logo:hover {
            transform: scale(1.05);
        }

        h4 {
            font-size: 2.8em; /* Even larger, more commanding heading */
            font-weight: 800; /* Extra bold */
            color: #1a202c;
            text-align: center;
            position: relative;
            padding-bottom: 25px; /* More space for underline */
            margin-bottom: 50px; /* More space below heading */
            text-shadow: 0 3px 8px rgba(0,0,0,0.08); /* More pronounced text shadow */
        }
        h4::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px; /* Wider, more impactful underline */
            height: 8px; /* Thicker underline */
            background: linear-gradient(to right, #6a11cb, #2575fc); /* Primary gradient */
            border-radius: 5px; /* More rounded underline */
        }

        .form-group {
            margin-bottom: 35px; /* Increased spacing between form groups */
        }

        .form-control {
            width: 100%;
            padding: 22px 28px; /* INCREASED padding for larger input */
            border: 1px solid #e0e0e0;
            border-radius: 20px;
            font-size: 1.2em; /* INCREASED font size for larger input */
            color: #333;
            background-color: #ffffff;
            transition: all 0.3s ease;
            outline: none;
            box-shadow: inset 0 3px 8px rgba(0, 0, 0, 0.08);
        }
        .form-control::placeholder {
            color: #a8a8a8;
        }
        .form-control:focus {
            border-color: #2575fc;
            box-shadow: 0 0 0 6px rgba(37, 117, 252, 0.35), inset 0 3px 8px rgba(0, 0, 0, 0.12);
            background-color: #fff;
        }

        .btn-primary {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            color: #fff;
            padding: 18px 35px; /* DECREASED padding for smaller button */
            border-radius: 18px; /* Slightly less rounded for smaller button */
            font-weight: 700;
            font-size: 1.25em; /* DECREASED font size for smaller button */
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1.25);
            border: none;
            cursor: pointer;
            box-shadow: 0 10px 35px rgba(37, 117, 252, 0.45); /* Adjusted shadow */
            width: 80%; /* DECREASED width for smaller button */
            max-width: 300px; /* Set a max-width for the button */
            display: flex;
            align-items: center;
            justify-content: center;
            text-transform: uppercase;
            letter-spacing: 1.2px; /* Slightly less letter spacing */
            margin: 45px auto 0 auto; /* Centered with top margin */
            position: relative;
            overflow: hidden;
        }
        /* Button shimmer effect */
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 50%;
            height: 100%;
            background: rgba(255, 255, 255, 0.2);
            transform: skewX(-20deg);
            transition: all 0.7s cubic-bezier(0.25, 0.8, 0.25, 1);
        }
        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary i { margin-right: 15px; font-size: 1.4em; } /* Adjusted icon size and space */
        .btn-primary:hover {
            background: linear-gradient(45deg, #2575fc, #6a11cb);
            transform: translateY(-8px) scale(1.01); /* Less aggressive lift and scale on smaller button */
            box-shadow: 0 15px 50px rgba(37, 117, 252, 0.6);
        }

        .bottom-links-section {
            margin-top: 45px;
            padding-top: 40px;
            border-top: 1px solid #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
            flex-direction: column;
        }

        .bottom-links-section h6 {
            font-weight: 500;
            color: #78909c;
            font-size: 1.05em;
            margin-bottom: 0;
        }

        .bottom-links-section a {
            color: #2575fc;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s ease, text-shadow 0.2s ease;
        }
        .bottom-links-section a:hover {
            color: #6a11cb;
            text-decoration: underline;
            text-shadow: 0 1px 5px rgba(106, 17, 203, 0.2);
        }

        /* Responsive adjustments */
        @media (max-width: 640px) {
            .card { padding: 40px; border-radius: 25px; }
            .auth-logo { max-width: 130px; margin-bottom: 35px; }
            h4 { font-size: 2.4em; margin-bottom: 40px; padding-bottom: 20px; }
            h4::after { width: 70px; height: 6px; }
            .form-group { margin-bottom: 30px; }
            .form-control { padding: 18px 22px; font-size: 1.1em; margin-bottom: 25px;}
            .btn-primary {
                padding: 18px 35px;
                font-size: 1.25em;
                margin-top: 40px;
                width: 100%; /* Full width on smaller screens */
                max-width: none;
            }
            .bottom-links-section {
                gap: 18px;
                margin-top: 40px;
                padding-top: 35px;
            }
        }
    </style>
</head>

<body>
    <!-- Auth Form -->
    <div class="auth-main relative">
        <div class="auth-wrapper v1 flex items-center w-full h-full min-h-screen">
            <div class="auth-form flex items-center justify-center grow flex-col min-h-screen relative p-6">
                <div class="w-full max-w-[550px] relative">
                    <!-- Background Blobs -->
                    <div class="background-blobs">
                        <span class="absolute"></span>
                        <span class="absolute"></span>
                        <span class="absolute"></span>
                        <span class="absolute"></span>
                    </div>

                    <div class="card sm:my-12 w-full shadow-none">
                        <div class="card-body !p-10">
                            <div class="text-center">
                                <a href="#">
                                    <img src="https://placehold.co/180x60/f0faff/333333?text=Your+Logo" alt="logo" class="auth-logo" />
                                </a>
                            </div>
                            <h4>Reset Password</h4>

                            <!-- Reset Form Start -->
                            <form action="/checkEmail" method="POST">
                                <div class="form-group">
                                    <input type="email" name="resetEmail" class="form-control" placeholder="Email Address" required />
                                </div>
                                <div class="text-center">
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-paper-plane"></i> Send OTP
                                    </button>
                                </div>
                            </form>
                            <!-- Reset Form End -->

                            <div class="bottom-links-section">
                                <h6 class="font-medium mb-0">Remember your password?</h6>
                                <a href="/signInPage" class="text-primary-500">Sign In</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
