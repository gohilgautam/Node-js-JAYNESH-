<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0, minimal-ui" />
    <title>OTP Verification</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: radial-gradient(circle at top left, #edf8ff 0%, #d4ebf5 40%, #c2dbe6 80%, #b0cfe0 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
            padding: 20px;
            box-sizing: border-box;
            position: relative;
        }

        .background-blobs span {
            content: '';
            position: absolute;
            display: block;
            border-radius: 50%;
            filter: blur(250px);
            z-index: -1;
            opacity: 0.3;
            animation: blobFloating 18s infinite alternate ease-in-out;
            will-change: transform, opacity;
        }

        .background-blobs span:nth-child(1) {
            top: -250px; left: -250px; width: 600px; height: 600px;
            background: rgba(173, 216, 230, 0.55);
            animation-delay: 0s;
        }
        .background-blobs span:nth-child(2) {
            bottom: -280px; right: -280px; width: 650px; height: 650px;
            background: rgba(255, 245, 180, 0.25);
            animation-delay: 5s;
        }
        .background-blobs span:nth-child(3) {
            top: 100px; right: -180px; width: 300px; height: 300px;
            background: rgba(135, 206, 250, 0.45);
            animation-delay: 10s;
        }
         .background-blobs span:nth-child(4) {
            bottom: -100px; left: -150px; width: 250px; height: 250px;
            background: rgba(220, 190, 255, 0.35);
            animation-delay: 15s;
        }

        @keyframes blobFloating {
            0% { transform: translate(0, 0) scale(1) rotate(0deg); }
            25% { transform: translate(40px, -45px) scale(1.06) rotate(5deg); }
            50% { transform: translate(-35px, 40px) scale(0.95) rotate(-5deg); }
            75% { transform: translate(45px, -30px) scale(1.05) rotate(3deg); }
            100% { transform: translate(0, 0) scale(1) rotate(0deg); }
        }

        .container {
            width: 100%;
            max-width: 500px;
            margin: auto;
            padding: 0;
        }

        .card {
            background: linear-gradient(155deg, rgba(255, 255, 255, 0.99), rgba(252, 252, 252, 0.99));
            border-radius: 30px;
            box-shadow: 0 25px 90px rgba(0, 0, 0, 0.18);
            padding: 55px;
            animation: fadeInScaleUp 0.9s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
            transition: transform 0.4s ease-in-out, box-shadow 0.4s ease-in-out;
            border: 1px solid rgba(255, 255, 255, 0.95);
            position: relative;
            z-index: 10;
            overflow: hidden;
            background-clip: padding-box;
            -webkit-backdrop-filter: blur(25px);
            backdrop-filter: blur(25px);
            perspective: 1000px;
        }
        .card:hover {
            transform: translateY(-12px);
            box-shadow: 0 35px 120px rgba(0, 0, 0, 0.25);
        }
        @keyframes fadeInScaleUp {
            from { opacity: 0; transform: translateY(50px) scale(0.88); }
            to { opacity: 1; transform: translateY(0) scale(1); }
        }

        .auth-logo {
            max-width: 180px;
            height: auto;
            margin: 0 auto 45px auto;
            display: block;
            transition: transform 0.3s ease;
        }
        .auth-logo:hover {
            transform: scale(1.05);
        }

        h4 {
            font-size: 1.8em;
            font-weight: 800;
            color: #1a202c;
            text-align: center;
            position: relative;
            padding-bottom: 25px;
            margin-bottom: 50px;
            text-shadow: 0 3px 8px rgba(0,0,0,0.08);
        }
        h4::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 8px;
            background: linear-gradient(to right, #6a11cb, #2575fc);
            border-radius: 5px;
            box-shadow: 0 3px 12px rgba(142, 45, 226, 0.5);
        }

        .form-group {
            margin-bottom: 35px;
        }

        .form-control {
            width: 100%;
            padding: 22px 28px;
            border: 1px solid #e0e0e0;
            border-radius: 20px;
            font-size: 1.2em;
            color: #333;
            background-color: #ffffff;
            transition: all 0.3s ease;
            outline: none;
            box-shadow: inset 0 3px 8px rgba(0, 0, 0, 0.08);
            text-align: center;
            letter-spacing: 0.2em;
        }
        .form-control::placeholder {
            color: #a8a8a8;
        }
        .form-control:focus {
            border-color: #2575fc;
            box-shadow: 0 0 0 6px rgba(37, 117, 252, 0.35), inset 0 3px 8px rgba(0, 0, 0, 0.12);
            background-color: #fff;
        }

        .btn-primary {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            color: #fff;
            padding: 18px 35px;
            border-radius: 18px;
            font-weight: 700;
            font-size: 1.25em;
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1.25);
            border: none;
            cursor: pointer;
            box-shadow: 0 10px 35px rgba(37, 117, 252, 0.45);
            width: 80%;
            max-width: 250px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            margin: 45px auto 0 auto;
            position: relative;
            overflow: hidden;
        }
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 50%;
            height: 100%;
            background: rgba(255, 255, 255, 0.2);
            transform: skewX(-20deg);
            transition: all 0.7s cubic-bezier(0.25, 0.8, 0.25, 1);
        }
        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary i { margin-right: 15px; font-size: 1.4em; }
        .btn-primary:hover {
            background: linear-gradient(45deg, #2575fc, #6a11cb);
            transform: translateY(-8px) scale(1.01);
            box-shadow: 0 15px 50px rgba(37, 117, 252, 0.6);
        }

        .bottom-links-section {
            margin-top: 45px;
            padding-top: 40px;
            border-top: 1px solid #f5f5f5;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 15px;
        }

        .bottom-links-section p {
            margin-bottom: 0;
            color: #78909c;
            font-size: 0.95em;
        }

        .bottom-links-section a {
            color: #2575fc;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s ease, text-shadow 0.2s ease;
        }
        .bottom-links-section a:hover {
            color: #6a11cb;
            text-decoration: underline;
            text-shadow: 0 1px 5px rgba(106, 17, 203, 0.2);
        }

        @media (max-width: 640px) {
            .card { padding: 40px; border-radius: 25px; }
            h4 { font-size: 2.4em; margin-bottom: 40px; padding-bottom: 20px; }
            h4::after { width: 70px; height: 6px; }
            .form-group { margin-bottom: 30px; }
            .form-control { padding: 18px 22px; font-size: 1.1em; margin-bottom: 25px;}
            .btn-primary {
                padding: 18px 35px;
                font-size: 1.25em;
                width: 100%;
                max-width: none;
                margin-top: 40px;
            }
            .bottom-links-section {
                gap: 12px;
                margin-top: 35px;
                padding-top: 30px;
            }
        }
    </style>
</head>

<body>
    <div class="auth-main relative">
        <div class="auth-wrapper v1 flex items-center w-full h-full min-h-screen">
            <div class="auth-form flex items-center justify-center grow flex-col min-h-screen relative p-6">
                <div class="w-full max-w-[500px] relative">
                    <div class="background-blobs">
                        <span></span>
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>

                    <div class="card sm:my-12 w-full shadow-none">
                        <div class="card-body !p-10">
                            <div class="text-center mb-8">
                                <img src="https://placehold.co/180x60/edf8ff/333333?text=Your+Logo" alt="logo" class="auth-logo" />
                            </div>
                            <h4>OTP Verification</h4>

                            <form action="/verifyOTP" method="POST">
                                <div class="form-group">
                                    <input type="text" name="OTP" class="form-control" placeholder="Enter 6-digit OTP" maxlength="6" required />
                                </div>
                                <div class="text-center">
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-check-circle"></i> Verify OTP
                                    </button>
                                </div>
                            </form>

                            <div class="bottom-links-section">
                                <p>
                                    Didn’t receive the OTP?
                                    <a href="/resend-otp">Resend OTP</a>
                                </p>
                                <p>
                                    Remembered your password?
                                    <a href="/sign-in">Sign In</a>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
