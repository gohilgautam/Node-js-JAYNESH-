<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Set Password</title>
    <!-- Tailwind CSS for utility classes and responsive design -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Font: Inter (preferred font) -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Deeper, more complex background gradient with a hint of warmth and dynamic feel */
            background: radial-gradient(circle at top left, #edf8ff 0%, #d4ebf5 40%, #c2dbe6 80%, #b0cfe0 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
            padding: 20px;
            box-sizing: border-box;
            position: relative;
        }

        /* Further enhanced subtle background blobs for visual depth and gentle shimmer */
        .background-blobs span {
            content: '';
            position: absolute;
            display: block;
            border-radius: 50%;
            filter: blur(250px); /* Even more blur for ultimate softness */
            z-index: -1;
            opacity: 0.3; /* Even less opaque for true ethereal feel */
            animation: blobFloating 18s infinite alternate ease-in-out; /* Slower, smoother animation */
            will-change: transform, opacity; /* Performance optimization */
        }

        .background-blobs span:nth-child(1) {
            top: -250px; left: -250px; width: 600px; height: 600px;
            background: rgba(173, 216, 230, 0.55); /* Light blue, slightly more vibrant */
            animation-delay: 0s;
        }
        .background-blobs span:nth-child(2) {
            bottom: -280px; right: -280px; width: 650px; height: 650px;
            background: rgba(255, 245, 180, 0.25); /* Softer yellow tint */
            animation-delay: 5s;
        }
        .background-blobs span:nth-child(3) {
            top: 100px; right: -180px; width: 300px; height: 300px;
            background: rgba(135, 206, 250, 0.45); /* Sky blue */
            animation-delay: 10s;
        }
         .background-blobs span:nth-child(4) {
            bottom: -100px; left: -150px; width: 250px; height: 250px;
            background: rgba(220, 190, 255, 0.35); /* Hint of lavender */
            animation-delay: 15s;
        }

        @keyframes blobFloating {
            0% { transform: translate(0, 0) scale(1) rotate(0deg); }
            25% { transform: translate(40px, -45px) scale(1.06) rotate(5deg); } /* More movement and subtle rotation */
            50% { transform: translate(-35px, 40px) scale(0.95) rotate(-5deg); }
            75% { transform: translate(45px, -30px) scale(1.05) rotate(3deg); }
            100% { transform: translate(0, 0) scale(1) rotate(0deg); }
        }

        .container {
            width: 100%;
            max-width: 600px; /* Consistent max-width for a more substantial card */
            margin: auto;
            padding: 0;
        }

        .card {
            background: linear-gradient(155deg, rgba(255, 255, 255, 0.99), rgba(252, 252, 252, 0.99)); /* Subtle gradient for card background */
            border-radius: 40px; /* Even more prominent rounded corners for a luxurious feel */
            box-shadow: 0 35px 120px rgba(0, 0, 0, 0.25); /* Deeper, more elegant shadow */
            padding: 65px; /* Increased padding for luxurious feel */
            animation: fadeInScaleUp 1.1s cubic-bezier(0.2, 0.8, 0.2, 1.3) forwards; /* Smoother, bouncier entrance */
            transition: transform 0.6s cubic-bezier(0.25, 0.8, 0.25, 1), box-shadow 0.6s cubic-bezier(0.25, 0.8, 0.25, 1);
            border: 1px solid rgba(255, 255, 255, 0.99); /* A subtle, almost invisible border for depth */
            position: relative;
            z-index: 10;
            overflow: hidden;
            background-clip: padding-box;
            -webkit-backdrop-filter: blur(35px); /* Enhanced blur */
            backdrop-filter: blur(35px);
            perspective: 1000px; /* For subtle 3D effect on hover */
        }
        .card:hover {
            transform: translateY(-18px) rotateX(2deg) rotateY(1deg); /* More significant lift and subtle 3D tilt */
            box-shadow: 0 45px 180px rgba(0, 0, 0, 0.35); /* More intense, diffused shadow on hover */
        }
        @keyframes fadeInScaleUp {
            from { opacity: 0; transform: translateY(70px) scale(0.8); } /* More initial movement and smaller scale */
            to { opacity: 1; transform: translateY(0) scale(1); }
        }

        h4 {
            font-size: 3.5em; /* Even larger, more commanding heading */
            font-weight: 900; /* Ultra bold */
            color: #1a202c;
            text-align: center;
            position: relative;
            padding-bottom: 32px; /* More space for underline */
            margin-bottom: 60px; /* More space below heading */
            text-shadow: 0 5px 12px rgba(0,0,0,0.15); /* More pronounced text shadow */
        }
        h4::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 140px; /* Wider, more impactful underline */
            height: 10px; /* Thicker underline */
            background: linear-gradient(to right, #8e2de2, #4a00e0); /* A more striking, bold gradient with deeper purples */
            border-radius: 7px; /* More rounded underline */
            box-shadow: 0 3px 12px rgba(142, 45, 226, 0.5); /* Subtle glow for underline */
        }

        .form-group {
            margin-bottom: 45px; /* Increased spacing between form groups */
        }

        .form-control {
            width: 100%;
            padding: 16px 35px; /* Even more generous padding for inputs */
            border: 1px solid #e0e0e0; /* Finer border */
            border-radius: 30px; /* Even more rounded inputs */
            font-size: 1.35em; /* Larger text in inputs */
            color: #333;
            background-color: #ffffff; /* Pure white input background */
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
            outline: none;
            box-shadow: inset 0 5px 12px rgba(0, 0, 0, 0.1); /* Deeper, softer inner shadow */
        }
        .form-control::placeholder {
            color: #b8b8b8; /* Softer placeholder */
            transition: opacity 0.3s ease;
        }
        .form-control:focus::placeholder {
            opacity: 0.5; /* Fade placeholder on focus */
        }
        .form-control:focus {
            border-color: #4a00e0; /* Primary purple on focus */
            box-shadow: 0 0 0 10px rgba(74, 0, 224, 0.2), inset 0 5px 12px rgba(0, 0, 0, 0.18); /* More vibrant, diffused focus glow + inner shadow */
            background-color: #fff;
        }

        .btn-confirm {
            background: linear-gradient(45deg, #8e2de2, #4a00e0); /* Main gradient, matching heading underline */
            color: #fff;
            padding: 20px 40px; /* Decreased padding for smaller button */
            border-radius: 20px; /* Less rounded for smaller button */
            font-weight: 700;
            font-size: 1.25em; /* Decreased font size for smaller button */
            transition: all 0.6s cubic-bezier(0.25, 0.8, 0.25, 1.25); /* Smoother, bouncier transition */
            border: none;
            cursor: pointer;
            box-shadow: 0 15px 50px rgba(142, 45, 226, 0.55); /* Deeper, more vibrant shadow */
            width: 60%; /* Further decreased width */
            max-width: 220px; /* Set a strict max-width */
            display: flex;
            align-items: center;
            justify-content: center;
            text-transform: uppercase;
            letter-spacing: 1.5px; /* Slightly more letter spacing for impact */
            margin-left: auto; /* Push to right */
            margin-right: auto; /* CENTERED BUTTON */
            position: relative;
            overflow: hidden;
            box-sizing: border-box; /* Ensure padding/border included in width */
            /* Add subtle 3D tilt on hover */
            transform-style: preserve-3d;
            transform: translateZ(0);
        }
        /* Button shimmer effect */
        .btn-confirm::before {
            content: '';
            position: absolute;
            top: 0;
            left: -120%; /* Start further left */
            width: 60%; /* Wider shimmer */
            height: 100%;
            background: rgba(255, 255, 255, 0.35); /* Stronger shimmer */
            transform: skewX(-25deg); /* More aggressive skew */
            transition: all 0.8s cubic-bezier(0.25, 0.8, 0.25, 1);
        }
        .btn-confirm:hover::before {
            left: 120%; /* End further right */
        }

        .btn-confirm i { margin-right: 15px; font-size: 1.4em; } /* Slightly larger icon to match button size */
        .btn-confirm:hover {
            background: linear-gradient(45deg, #4a00e0, #8e2de2); /* Reverse gradient on hover */
            transform: translateY(-12px) scale(1.03) rotateX(3deg); /* More pronounced lift, slight scale, and subtle 3D tilt */
            box-shadow: 0 20px 65px rgba(142, 45, 226, 0.8); /* Even stronger shadow on hover */
        }

        .bottom-section {
            margin-top: 55px; /* Increased space */
            padding-top: 50px;
            border-top: 1px solid #f8f8f8; /* Even lighter, cleaner separator */
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 30px; /* Increased space if wrapped */
        }

        .back-link {
            color: #7a7a7a; /* Softer gray text */
            text-decoration: none;
            font-weight: 500;
            font-size: 1.15em; /* Slightly larger */
            transition: color 0.4s ease, text-shadow 0.4s ease;
            display: flex;
            align-items: center;
        }
        .back-link i {
            margin-right: 12px; /* More space */
            font-size: 1.3em; /* Slightly larger icon */
            color: #a0a0a0; /* Subtle icon color */
            transition: color 0.4s ease, transform 0.4s ease;
        }
        .back-link:hover {
            color: #8e2de2; /* Matches button gradient start */
            text-decoration: underline;
            text-shadow: 0 2px 8px rgba(142, 45, 226, 0.4); /* Subtle glow on hover */
        }
        .back-link:hover i {
            color: #4a00e0; /* Matches button gradient end */
            transform: translateX(-5px); /* Subtle icon slide */
        }

        /* Responsive adjustments */
        @media (max-width: 640px) {
            .card { padding: 45px; border-radius: 30px; }
            h4 { font-size: 2.8em; margin-bottom: 50px; padding-bottom: 25px; }
            h4::after { width: 90px; height: 8px; }
            .form-group { margin-bottom: 35px; }
            .form-control { padding: 22px 28px; font-size: 1.2em; margin-bottom: 30px;}
            .btn-confirm {
                padding: 18px 35px;
                font-size: 1.2em;
                width: 100%;
                max-width: none;
                margin-left: auto;
                margin-right: auto;
            }
            .bottom-section {
                flex-direction: column-reverse;
                gap: 35px;
                margin-top: 45px;
                padding-top: 40px;
            }
        }
    </style>
</head>

<body>
    <div class="auth-main relative">
        <div class="auth-wrapper v1 flex items-center w-full h-full min-h-screen">
            <div class="auth-form flex items-center justify-center grow flex-col min-h-screen relative p-6">
                <div class="w-full max-w-[500px] relative">
                    <!-- Background Blobs -->
                    <div class="background-blobs">
                        <span class="absolute"></span>
                        <span class="absolute"></span>
                        <span class="absolute"></span>
                        <span class="absolute"></span>
                    </div>

                    <div class="card sm:my-12 w-full shadow-none">
                        <div class="card-body !p-10">
                            <h4>New Set Password</h4>
                            <form method="post" action="/checknewpassword">
                                <div class="form-group">
                                    <input type="password" name="newPassword" class="form-control" placeholder="Enter New Password" required />
                                </div>
                                <div class="form-group">
                                    <input type="password" name="confirmPassword" class="form-control" placeholder="Confirm New Password" required />
                                </div>
                                <div class="bottom-section">
                                    <a href="/" class="back-link">
                                        <i class="fas fa-arrow-left"></i> Back To Login
                                    </a>
                                    <button type="submit" class="btn-confirm">
                                        Confirm <i class="fas fa-check"></i>
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
